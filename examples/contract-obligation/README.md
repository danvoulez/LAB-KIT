# contract-obligation

Example package for a production LogLine Lab Kit flow. Examples must emit LogLine Acts to `ops.logline_acts` when connected to Supabase/Postgres and must not treat local JSON as truth.
